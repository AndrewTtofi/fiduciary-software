import type { TemplateSite } from "@/components/marketing/templates/types";
import { Header } from "./Header";
import { Footer } from "./Footer";
import { Landing } from "./Landing";

/** Atelier — boutique editorial site: typographic hero, numbered service
 *  index, hairlines and square corners throughout. */
export const atelier: TemplateSite = {
  Header,
  Footer,
  Landing,
  fx: false,
};
